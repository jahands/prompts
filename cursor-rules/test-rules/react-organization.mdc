---
description: React component organization - size limits, hook extraction, test colocation
globs:
alwaysApply: false
---
<react-component-organization>

<title>React Component Organization</title>

<rules>
- Keep components under 200 lines
- Extract custom hooks to separate files in same directory
- Colocate test files with components using `.test.tsx` or `.spec.tsx` suffix
- Place hook files alongside components with `.hooks.ts` suffix
- Split large components into smaller sub-components
- Keep component logic focused on single responsibility
</rules>

<examples>
<example type="good">
```
src/components/UserProfile/
├── UserProfile.tsx          // Main component (< 200 lines)
├── UserProfile.test.tsx     // Colocated tests
├── UserProfile.hooks.ts     // Extracted custom hooks
└── UserProfileAvatar.tsx    // Sub-component
```
</example>
</examples>

</react-component-organization>