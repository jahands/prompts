---
description: Organize imports with external packages first, internal modules second, relative imports last, with blank lines between groups
globs:
alwaysApply: false
---
<import-ordering>

<title>Import Organization</title>

<rules>
- Group imports in this order: external packages, internal modules, relative imports
- Add a blank line between each import group
- Sort imports alphabetically within each group
- External packages: from node_modules (e.g., `react`, `lodash`)
- Internal modules: absolute imports from project (e.g., `@/components`, `src/utils`)
- Relative imports: start with `./` or `../`
</rules>

<examples>
<example type="good">
```typescript
// External packages
import React from 'react';
import { useState } from 'react';
import lodash from 'lodash';

// Internal modules
import { Button } from '@/components/ui';
import { useAuth } from 'src/hooks/auth';

// Relative imports
import { formatDate } from './utils';
import { UserCard } from '../components/UserCard';
```
</example>
</examples>

</import-ordering>