---
description: Organize imports into three groups: external packages, internal modules, relative imports
globs:
alwaysApply: false
---
<import-ordering>

<title>Import Organization Rules</title>

<rules>
- Group imports into three distinct sections
- Order: external packages first, then internal modules, then relative imports
- Add one blank line between each import group
- Sort imports alphabetically within each group
- External packages: installed via npm/yarn (e.g., `react`, `lodash`)
- Internal modules: absolute imports from project (e.g., `@/components`, `src/utils`)
- Relative imports: start with `.` or `..` (e.g., `./utils`, `../components`)
</rules>

<examples>
<example type="good">
```typescript
// External packages
import React from 'react';
import { useState, useEffect } from 'react';
import axios from 'axios';
import lodash from 'lodash';

// Internal modules
import { Button } from '@/components/Button';
import { useAuth } from '@/hooks/auth';
import { apiClient } from 'src/services/api';

// Relative imports
import { formatDate } from './utils';
import { LOCAL_CONFIG } from './constants';
import type { UserProfile } from '../types';
```
</example>
</examples>

</import-ordering>