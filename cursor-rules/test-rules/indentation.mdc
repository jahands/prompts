---
description: JavaScript/TypeScript indentation standards using 2 spaces
globs:
alwaysApply: false
---
<indentation-standards>

<title>JavaScript/TypeScript Indentation</title>

<rules>
- Always use 2 spaces for indentation
- Never use tabs
- Configure editors to insert spaces when tab key is pressed
- Each nested level increases indentation by exactly 2 spaces
- Maintain consistent indentation throughout the entire file
- Use 2 spaces for continuation lines
</rules>

<examples>
<example type="good">
```typescript
function processData(items: string[]) {
  if (items.length > 0) {
    return items.map(item => {
      const processed = item.trim();
      return processed.toUpperCase();
    });
  }
  
  return [];
}

class UserService {
  constructor(
    private readonly database: Database,
    private readonly logger: Logger
  ) {}
  
  async getUser(id: string): Promise<User> {
    try {
      const user = await this.database.findById(id);
      return user;
    } catch (error) {
      this.logger.error('Failed to get user', error);
      throw error;
    }
  }
}
```
</example>

<example type="bad">
```typescript
function processData(items: string[]) {
	if (items.length > 0) {  // Using tabs
		return items.map(item => {
    const processed = item.trim();  // Inconsistent spacing
			return processed.toUpperCase();  // Mixed tabs and spaces
		});
	}
	
	return [];
}
```
</example>
</examples>

</indentation-standards>