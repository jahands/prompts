---
description: JavaScript/TypeScript indentation standards - always use 2 spaces, never tabs
globs:
alwaysApply: false
---
<indentation-standards>

<title>JavaScript/TypeScript Indentation</title>

<rules>
- Always use 2 spaces for indentation
- Never use tabs
- Configure editors to insert spaces when tab key is pressed
- Each nested level increases indentation by exactly 2 spaces
- Maintain consistent indentation throughout the entire file
</rules>

<examples>
<example type="good">
```typescript
function processData(items: string[]) {
  if (items.length > 0) {
    items.forEach((item) => {
      console.log(item);
    });
  }
}

class UserService {
  private users: User[] = [];

  async getUser(id: string): Promise<User> {
    const user = this.users.find(u => u.id === id);
    if (!user) {
      throw new Error('User not found');
    }
    return user;
  }
}
```
</example>
</examples>

</indentation-standards>