---
description: Enforce 2-space indentation in JavaScript/TypeScript files
globs:
alwaysApply: false
---
<indentation-standards>

<title>JavaScript/TypeScript Indentation</title>

<rules>
- Always use 2 spaces for indentation
- Never use tabs
- Configure editors to insert spaces for tab key
- Each nested level increases by 2 spaces
</rules>

<examples>
<example type="good">
```typescript
function processData(items: string[]) {
  if (items.length > 0) {
    return items.map(item => {
      return item.trim();
    });
  }
}
```
</example>
</examples>

</indentation-standards>